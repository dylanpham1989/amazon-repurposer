# Secrets — TUYỆT ĐỐI không commit
.env
.env.local
.env.*.local

# Toàn bộ data/ — chứa:
#   - app.db (SQLite cũ)
#   - fixtures/*.html: trang Amazon đã lưu, 2.4MB, NỘI DUNG CÓ BẢN QUYỀN → không đẩy lên GitHub
#   - assets/*.png: sticker cache (giờ lưu trong Postgres)
data/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/

# macOS
.DS_Store

# Ảnh/HTML tạm khi debug
tmp/
out/
